Requirements for using this

You need to have python and pip already installed
	For Mac:
		sudo easy_install pip

	For Linux:
		sudo apt-get install python-pip
		In case it does not work
			curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
			python2 get-pip.py

Then install:
	sudo pip virtualenv

Create an env
	virtualenv .env

Activate env
	source .env/bin/activate

Install dependencies
	pip install -r requirements.txt


Give the permissions necessary to do.sh and execute 
	./do.sh		

Done !	

-------------------------------------

For easier use install xclip and use this command in the htmlFolder
	xclip -out -sel clip  > `date +%s`
